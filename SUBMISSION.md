# 제출 준비 메모 (초안 — 배포 후 채워 주세요)

요약: 구현과 합성 검증(9개 fixture 전부 통과, 17개 원본 파일 해시 전량 일치, 비밀값 0건)까지 마친 초안입니다. 실제 서로 다른 두 날짜의 기록은 배포 뒤에 생깁니다.

## 주소

- 결과물 주소: `https://<사용자명>.github.io/<저장소명>/` ← 배포 후 채우기
- 소스: 결과물 화면 하단 &ldquo;소스 저장소&rdquo; 링크(전체 커밋 식별자 포함) 또는 `https://github.com/<사용자명>/<저장소명>`

## 짧은 확인 방법 4줄

1. 위치: 결과물 주소의 &ldquo;관측 기록&rdquo;과 &ldquo;수신 테스트&rdquo;로 갑니다.
2. 행동: 실패 종류 하나 선택 → &ldquo;다시 시도 · 합성 복구&rdquo; → &ldquo;관측 기록&rdquo;에서 &ldquo;원자료 보기&rdquo;를 누릅니다.
3. 통과: 실패 시 105 pt·stale·1행 유지, 복구 시 120 pt·fresh/none·2행(+15). 실제 기록은 서로 다른 KST 날짜 2건이며 원자료·저장값·화면값과 전일 대비 계산이 일치합니다.
4. 안 될 때: 실패 이유·오래된 값 표시·다시 시도 버튼이 보입니다. 실제 기록이 한 건뿐이면 &ldquo;다음 실제 날짜 · 수집 대기&rdquo; 행이 보입니다.

## AI와 나의 판단 3줄

1. AI에게 맡긴 일: 엔진 이식(9개 fixture 검증 포함), Open-Meteo 연동, 화면 구현, 일별 저장·실패·복구 처리, 테스트·배포 워크플로 구성.
2. 직접 판단한 일: _(예: 서울 기온을 주제로 선택했습니다. 강조색을 OOO로 지정했습니다. — 직접 적어 주세요)_
3. AI 제안을 따르지 않은 일: _(있다면 적고, 없다면 그 이유를 적어 주세요)_

## 제출 전 반드시 확인할 것

- [ ] `REPO_URL`(`app.mjs`)을 실제 저장소 주소로 바꿨다.
- [ ] GitHub Pages가 배포되어 있고, 결과물·소스 URL을 **새 시크릿 창**에서 로그인 없이 열어봤다.
- [ ] 워크플로를 최소 1회 수동 실행(`collect` 체크)해 첫 실제 기록을 만들었다.
- [ ] 다른 KST 날짜에 한 번 더 성공 수집해 서로 다른 날짜 2건을 채웠다(예약 실행 또는 재수동 실행 — 최소 하루 대기 필요).
- [ ] &ldquo;AI와 나의 판단&rdquo; 2·3번을 본인 언어로 채웠다.
- [ ] 첨부 `public-contract.json`이 말하는 `t04_day` 플랫폼 영수증 2건의 발급 절차는 이 자료에 없으므로, 실제 제출 화면의 안내를 따로 확인한다.
